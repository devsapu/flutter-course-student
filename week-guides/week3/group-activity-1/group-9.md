# Group 9 — Activity 1 (core)

**Branch:** `week3-a1-group-9/jsonplaceholder-albums`

---

## Your API (only yours)

**Endpoint:** `https://jsonplaceholder.typicode.com/albums`  
**Tip:** `?_limit=20` keeps it light.

**Expected JSON:** A **JSON array** of albums.

**Each item often has:**

| Field | Type | Notes |
|--------|------|--------|
| `id` | int | |
| `userId` | int | Optional subtitle |
| `title` | String | Main line in list |

---

## What everyone must implement (same for all groups)

1. **Fetch** + parse array.
2. **Display** — **title** + optional `userId`.
3. **Loading** / **Error** / **Empty state**
4. **Provider**
5. **Refresh**
6. **(Optional)** Response time

---

## Steps

1. Branch: `week3-a1-group-9/jsonplaceholder-albums`
2. Commit, push, PR

---

## Help

- Same pattern as todos/posts — straight **List** from `jsonDecode`.
